# .env.example

# Replace 'youruser', 'yourpassword', and 'yourdb' with your actual credentials in .env
DATABASE_URL=postgresql://youruser:yourpassword@localhost:5432/yourdb

# Use the local model path if running offline, or switch to a Hugging Face model ID
EMBEDDING_MODEL=models/local/BAAI-bge-m3

# Directory where structured text files are stored
CHUNK_DIR=data/full_text/structured
